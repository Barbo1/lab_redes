/* Para el servidor. */

asumimos que tenemos las siguientes funciones:

get_json_rpc_right(version, resultado, id);

retorna un json rpc que representa una salida valida, con un resultado.

get_json_rpc_error(version, code, id);

retorna un json rpc que representa una salida invalida, con un codigo y mensaje 
de error correspondientes.

validate_json_rpc(json);

retorna true si el json es un json_rpc valido.



class Server {
  
}
